const express = require("express");
const router = express.Router();

// fetch all the coderooms joined by the user
router.post("/studentCoderooms", () => {});

// fetch assignment details of a particular coderoom
router.post("/studentCoderoomDetails");

